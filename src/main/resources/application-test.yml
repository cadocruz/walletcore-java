spring:
  datasource:
    driver-class-name: org.h2.Driver
    username: root
    password: root
    url: jdbc:h2:mem:wallet_test;MODE=MYSQL;DATABASE_TO_LOWER=TRUE
  h2:
    console:
      enabled: true
      path: /h2
  flyway:
    enabled: false
  kafka:
    bootstrap-servers: localhost:9092
    admin:
      auto-create: true